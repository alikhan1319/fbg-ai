# Core API
fastapi>=0.110.0
uvicorn>=0.27.0
python-multipart>=0.0.9

# Image stack — numpy<2 required for torch 2.1.2 + Real-ESRGAN/basicsr
numpy>=1.26.0,<2.0.0
Pillow>=9.5.0,<10.0.0
opencv-python-headless==4.9.0.80

# Segmentation: SAM2 prior → BiRefNet → PyMatting
# rembg kept as BiRefNet fallback while torch stays on 2.1.x (transformers 5 needs torch>=2.4)
pymatting>=1.1.0
rembg==2.0.59
# Optional SAM2 (falls back to OpenCV prior if missing):
# ultralytics>=8.3.0
# Optional HF BiRefNet when torch>=2.4:
# transformers>=4.40.0,<5.0
# timm>=0.9.0

# AI upscale / face restore (CPU wheels)
torch==2.1.2
torchvision==0.16.2
basicsr==1.4.2
realesrgan==0.3.0
gfpgan==1.3.8
tqdm>=4.67.0

# Watermark inpainting
simple-lama-inpainting>=0.1.2

# CMS / MySQL
sqlalchemy>=2.0.0
pymysql>=1.1.0
cryptography>=42.0.0
email-validator>=2.0.0
